# Опционально: для локального npm run build (без Docker), скопируйте в packages-ui/*/.env.production
# Образ Docker больше не требует VITE_* — домены задаются на API в runtime (см. deploy/.env.example).
